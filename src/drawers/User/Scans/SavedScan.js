import { ScrollView, StyleSheet, Text, View } from "react-native";
import React from "react";

const SavedScan = () => {
  return (
    <ScrollView>
      <Text>Your Scanned Card</Text>
    </ScrollView>
  );
};

export default SavedScan;

const styles = StyleSheet.create({});
